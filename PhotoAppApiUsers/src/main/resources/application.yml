server:
  port: ${PORT:0}

gateway:
  ip: host.docker.internal

login:
  path: /users/login

jwt:
  secretKey: fh279rfhf0923yrfwhfk2fh8923hjfkfheowf8123gfhidjfnv384jvaodfeb83gvbu42r93jbknl
  authorizationHeader: Authorization
  tokenPrefix: Bearer
  tokenExpirationDays: 10

spring:
  application:
    name: users-ws

  devtools:
    restart:
      enabled: true

  datasource:
    url: jdbc:h2:~/database/testdb

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  jackson:
    property-naming-strategy: SNAKE_CASE

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka

  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}